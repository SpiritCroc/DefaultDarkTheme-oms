<?xml version="1.0" encoding="utf-8"?>

<resources>

    <!-- BG: open with... -->
    <color name="resolver_list_bg">@android:color/white</color>

    <!-- BG: share with -->
    <color name="chooser_service_row_background_color">#fff5f5f5</color>

    <style name="Theme.DeviceDefault.Resolver" parent="@android:style/Theme.Material.Light">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:colorControlActivated">#1e000000</item><!-- ?attr/colorControlHighlight -> ripple_material_dark -->
    <item name="android:listPreferredItemPaddingStart">16dp</item><!-- ?attr/dialogPreferredPadding -> list_item_padding_horizontal_material -> action_bar_content_inset_material -->
    <item name="android:listPreferredItemPaddingEnd">16dp</item><!-- ?attr/dialogPreferredPadding -> list_item_padding_horizontal_material -> action_bar_content_inset_material -->
    </style>
</resources>
